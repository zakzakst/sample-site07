<!-- ▼▼▼ ページローダー ▼▼▼ -->
<style>
.page-loader {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 10000;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fff;
}

.dot-wave {
  --uib-size: 47px;
  --uib-speed: 1s;

  display: flex;
  flex-flow: row nowrap;
  align-items: center;
  justify-content: space-between;
  width: var(--uib-size);
  height: calc(var(--uib-size) * 0.17);
  padding-top: calc(var(--uib-size) * 0.34);
}

.dot-wave__dot {
  flex-shrink: 0;
  width: calc(var(--uib-size) * 0.17);
  height: calc(var(--uib-size) * 0.17);
  background-color: #b25900;
  border-radius: 50%;
  will-change: transform;
}

.dot-wave__dot:nth-child(1) {
  animation: jump var(--uib-speed) ease-in-out calc(var(--uib-speed) * -0.45) infinite;
}

.dot-wave__dot:nth-child(2) {
  animation: jump var(--uib-speed) ease-in-out calc(var(--uib-speed) * -0.3) infinite;
}

.dot-wave__dot:nth-child(3) {
  animation: jump var(--uib-speed) ease-in-out calc(var(--uib-speed) * -0.15) infinite;
}

.dot-wave__dot:nth-child(4) {
  animation: jump var(--uib-speed) ease-in-out infinite;
}

@keyframes jump {
  0%,
  100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-200%);
  }
}
</style>

{# 「gulp-htmlmin」での圧縮が効かなかったため、元の記述をVScode側で圧縮しておく #}
{# <script>
  document.documentElement.style.overflow = 'hidden';
  setTimeout(() => {
    const docDisplay = document.getElementById('js-loader').style.display;
    if (!docDisplay === 'display') {
      docDisplay = 'none';
      document.documentElement.style.overflow = null;
    }
  }, 10000);
</script> #}

<script>document.documentElement.style.overflow = 'hidden';setTimeout(() => {const docDisplay = document.getElementById('js-page-loader').style.display;if (!docDisplay === 'display') {docDisplay = 'none';document.documentElement.style.overflow = null;}}, 10000);</script>
<!-- ▲▲▲ ページローダー ▲▲▲ -->